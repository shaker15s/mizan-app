# Optional. Blank means the production and staging apps fail closed
# and will not invent an ERP result.
MIZAN_API_BASE_URL=

# Release signing. If these are absent, release builds are unsigned.
# KEYSTORE_PATH=
# STORE_PASSWORD=
# KEY_PASSWORD=
# KEY_ALIAS=upload
